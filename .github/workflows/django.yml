name: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.7, 3.8, 3.9]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      run: |
        python manage.py test
    - name: Docker Action
      # You may pin to the exact commit or the version.
      # uses: zenato/docker-action@8309ea4d790d074676660449b9f2bcbcae25cdb9
      uses: zenato/docker-action@v1.0.0
      with:
        # Registry username
        username: djangodocker123
        # Registry password
        password: "#H9rYH5![,kdhJM"
        # Repository on registry
        repository: djangodocker123/djangoproject2022
        # Registry hostname
